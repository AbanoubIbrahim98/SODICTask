@model IEnumerable<SODICTask.Models.AttributeValue>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DynamicAttribute)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Product)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DynamicAttribute.DynamicAttributeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.ProductId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AttributeValueId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AttributeValueId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AttributeValueId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
